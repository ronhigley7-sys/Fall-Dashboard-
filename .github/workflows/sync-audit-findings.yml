name: Sync Audit Findings

on:
  push:
    paths:
      - scripts/sync-audit-findings.js
      - .github/workflows/sync-audit-findings.yml
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch: {}

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run audit findings sync
        env:
          SMARTSHEET_TOKEN: ${{ secrets.SMARTSHEET_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_KEY }}
          AUDIT_SMARTSHEET_SOURCES: ${{ vars.AUDIT_SMARTSHEET_SOURCES }}
          CHART_AUDIT_SHEET_ID: ${{ secrets.SMARTSHEET_CHART_AUDIT_SHEET_ID }}
          CHART_AUDIT_REPORT_ID: ${{ secrets.SMARTSHEET_CHART_AUDIT_REPORT_ID }}
          PAIN_REASSESSMENT_SHEET_ID: ${{ secrets.SMARTSHEET_PAIN_REASSESSMENT_SHEET_ID }}
          PAIN_REASSESSMENT_REPORT_ID: ${{ secrets.SMARTSHEET_PAIN_REASSESSMENT_REPORT_ID }}
          SITTER_AUDIT_SHEET_ID: ${{ secrets.SMARTSHEET_SITTER_AUDIT_SHEET_ID }}
          SITTER_AUDIT_REPORT_ID: ${{ secrets.SMARTSHEET_SITTER_AUDIT_REPORT_ID }}
          BEHAVIORAL_AUDIT_SHEET_ID: ${{ secrets.SMARTSHEET_BEHAVIORAL_AUDIT_SHEET_ID }}
          BEHAVIORAL_AUDIT_REPORT_ID: ${{ secrets.SMARTSHEET_BEHAVIORAL_AUDIT_REPORT_ID }}
        run: node scripts/sync-audit-findings.js
